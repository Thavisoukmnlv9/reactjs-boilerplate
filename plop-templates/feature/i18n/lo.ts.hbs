import type { TranslationShape } from '@/lib/i18n/types'

import type { en } from './en'

// TODO: translate to Lao (ລາວ).
export const lo: TranslationShape<typeof en> = {
  title: '{{titleCase name}}',
}
